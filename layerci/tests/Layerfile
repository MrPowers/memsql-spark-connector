FROM /layerci/Layerfile

# split to 9 states
# each of them will run different version of the memsql and spark
SPLIT 9

RUN echo testing

# export MEMSQL_IMAGE and SPARK_VERSION
# RUN ./scripts/execute-tests.sh $SPLIT